import styled from "styled-components";
import { theme } from "../../Theme";

export const CalendarTitleLabelStyle = styled.h1`
    width: fit-content;

    font-family: ${theme.font.inter};
    font-size: 2rem;

    color: ${theme.color.primary};

`;