import styled from "styled-components";
import { theme } from "../../../Theme";

export const CalendarDayLabelStyle = styled.h1`
    font-size: 1rem;
    color: ${theme.color.black};
    margin: 0;
`;