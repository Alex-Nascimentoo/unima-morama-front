import styled from "styled-components";
import { theme } from "../../../Theme";

export const BalanceParagraphStyle = styled.p`
    font-size: 0.75rem;
    color: ${theme.color.gray};
    margin: 0;
`